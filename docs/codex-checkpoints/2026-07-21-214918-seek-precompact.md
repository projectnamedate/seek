# Codex Checkpoint - seek - 2026-07-21-214918

> Created automatically by the global Codex `PreCompact` hook before history compaction and completed during the session wrap-up.

## Status

The reported repeat-win incident is contained and the hardened backend is live on the Helsinki VPS. The reported wallet and its soulbound SGT are permanently denied, and normal bounty admission is capped at two wins per UTC day per SGT-backed identity (wallet fallback). Repo implementation and handoff commits remain intentionally local-only.

## Goal

Permanently block wallet `3vw6SovWwMAWJeKEqeFuDG2JndEnNp3o7TqDWL4W2Cvv`, determine how it achieved five wins in six rapid hunts, and stop the same repeat-win pattern without disturbing unrelated session-proof work.

## Completed

- Reconstructed six Easy hunts in 13 minutes: five wins and one loss.
- Verified distinct, geographically coherent photos; evidence supports farming broad Easy missions from a target-rich location, not duplicate-photo replay or an auth bypass.
- Code-seeded the wallet and its soulbound SGT into the bounty denylist.
- Added a Redis-backed two-win daily cap, keyed by SGT when available and wallet otherwise, enforced at both `/prepare` and `/start`.
- Deployed the scoped backend update to the Mythx VPS and proved live `403` denial plus synthetic `429` cap enforcement.
- Removed one stale Redis `validating` record whose on-chain bounty PDA was already closed. No signing or fund movement occurred.
- Committed implementation as `d23bf3d` and the repo handoff as `4c65999`.

## Current State

- Repo: `/Users/hammer/Desktop/vibe/seek`
- Branch: `wip/session-proof-rollout`; no upstream; 16 commits ahead of `origin/master` and zero behind at wrap-up.
- HEAD before this checkpoint commit: `4c65999`.
- Pre-existing user WIP: `AGENTS.md` is modified and must not be staged, reverted, or stashed without operator direction.
- Existing stash preserved: `stash@{0}: On master: wip session-proof before ai hotfix rebase 2026-05-30`.
- Runtime: `seek-api` and `seek-redis` healthy; readiness HTTP `200` with RPC/program/Redis OK; pending `0`, validating `0`, finalizer queue `0` at the final incident sample.
- Deployed Compose config pins `MAX_WINS_PER_IDENTITY_PER_DAY=2`.
- `REQUIRE_BOUNTY_SESSION_PROOF=false` remains intentionally live pending the official-store smoke and explicit operator approval.
- Latest incident sample: house `87,888 SKR`, Singularity `12,600 SKR`, 6 won / 16 lost. Historical rate still includes the already-settled abuse wins.

## Decisions

- Permanently deny both the reported wallet and its verified soulbound SGT so the block follows the current identity.
- Use a two-win UTC-day cap because two wins across the existing 20-attempt allowance aligns with the 8-12% launch completion target; do not silently lower it to one.
- Treat the five-win event as physical-location farming of broad Easy targets based on the available evidence, not as proven forged imagery.
- Do not claim complete cross-wallet prevention while strict SGT session proof is disabled. A new unverified wallet receives a separate wallet-keyed allowance.
- Keep strict session-proof enforcement approval-gated until a store-installed v1.0.5 smoke passes.
- Keep the WIP branch local-only and preserve the existing stash and user-owned `AGENTS.md` edit.

## Changed Files

- `backend/src/services/bounty-blocklist.service.ts` - permanent wallet and SGT containment.
- `backend/src/services/wallet-bounty-limit.service.ts` - Redis-backed daily identity win accounting.
- `backend/src/routes/bounty.routes.ts` - admission checks and successful-win recording.
- `backend/src/services/redis.service.ts` and `backend/src/config/index.ts` - Redis key/config support.
- `backend/deploy/vps/compose.yaml` and `backend/.env.example` - pinned/default cap configuration.
- `backend/tests/bounty-blocklist*.test.ts` and `backend/tests/wallet-bounty-limit.test.ts` - containment and cap regression coverage.
- `tasks/todo.md` and `tasks/where-we-are.md` - verified incident review and cold-start handoff.
- `docs/codex-checkpoints/2026-07-21-214918-seek-precompact.md` - this completed checkpoint.

## Verification

- Backend launch-tool suite: PASS 59/59.
- Backend and mobile TypeScript checks: PASS.
- Contract mainnet `cargo check`: PASS with known Anchor cfg warnings.
- Contract tests: PASS 25/25; mission tests: PASS 6/6.
- dApp Store asset check and demo-residue scan: PASS.
- Route regression observed RED (`500`) before the admission guard and GREEN (`429`) after implementation.
- Live reported-wallet `/prepare` and `/start`: HTTP `403`.
- Live synthetic capped identity: HTTP `429`; temporary Redis proof key deleted and confirmed absent.
- Deployed source/config hashes matched local; Compose validation passed.
- Latest three sampled GitHub Actions runs on `master` were successful, but they do not cover the unpushed local commits.

## Risks / Open Items

- Strict session proof remains disabled. A determined person can rotate to a new wallet and receive a wallet-keyed cap unless the request has a verified SGT-backed session.
- Required next control: run the official store-installed v1.0.5 smoke, obtain explicit approval, then set `REQUIRE_BOUNTY_SESSION_PROOF=true` and verify old/custom-client rejection.
- Standard camera attestation is client-supplied and non-cryptographic until supported Seeker TEE attestation exists.
- Recheck real `403`/`429` traffic and `seek:win:daily:*` counters after 24 hours before changing the cap or mission difficulty.
- Sentry remains disabled; the VPS Redis volume has no encrypted off-host backup.
- The local WIP branch has no upstream and 16 unpushed commits. Do not push without explicit operator approval.

## Resume Prompt

```text
Resume Seek from /Users/hammer/Desktop/vibe/seek. First run /check-seek, then read tasks/where-we-are.md, /Users/hammer/Obsidian/AgentMemory/Projects/seek/seek.md, and docs/codex-checkpoints/2026-07-21-214918-seek-precompact.md. Preserve the user-owned AGENTS.md edit and stash@{0}. The next action is to inspect 24 hours of live 403/429 and seek:win:daily:* activity, then run the approval-gated official-store session-proof smoke before considering REQUIRE_BOUNTY_SESSION_PROOF=true.
```
